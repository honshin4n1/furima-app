-# あとで消す！！
= form_tag("/search", method: "get") do
  = label_tag(:q, "Search for:")
  = text_field_tag(:q)
  = submit_tag("Search")
          -# .search--icon
            -# = image_tag('material/icon/icon-search 1.png', height: '20px', width: '20px', alt: '検索アイコン')